/*
数组新增属性

Array.from() 将类似数组的对象或者继承了Symbol.iterator接口的对象转换成数组
Array.of() 通过多个参数转为数组
[].find(function(value, index, arr){return value}) 查找制定条件的值并返回
[].findIndex(function(value,index,arr){return index})  返回的是索引
[]copyWithin(position,start,end) 替换数组的内容 position替换位置，
即将替换的内容start起始索引，end替换的最后值
[].file()设置填充
*/
